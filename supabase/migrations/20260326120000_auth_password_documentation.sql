-- Senhas de login ficam em auth.users (Supabase Auth), hash automático.
-- Não armazene senhas em tabelas public.* (ex.: configuracoes).
-- O cliente aplica alteração com signInWithPassword + updateUser.

SELECT 1;
