project_id = "lsfwmbpvithxqerfdlhy"

[functions.webhook-solicitacao]
verify_jwt = false

# Permite preflight OPTIONS sem JWT; a função valida o token manualmente.
[functions.admin-users]
verify_jwt = false

[functions.self-upgrade-plan]
verify_jwt = true

# Consulta RDAP público (ex.: rdap.registro.br) — não exige sessão; o gateway ainda exige apikey (anon).
[functions.check-domain]
verify_jwt = false

# Proxy para Evolution API (evita 403 do navegador → servidor Evolution).
[functions.evolution-proxy]
verify_jwt = false

# QR do comunicador próprio: cria instância na Evolution do admin (credenciais no banco).
[functions.evolution-motorista-qr]
verify_jwt = false

[functions.evolution-motorista-sync]
verify_jwt = false

[functions.evolution-motorista-delete]
verify_jwt = false

# Proxy para webhook n8n (evita CORS do navegador → n8n).
[functions.n8n-comunicar-proxy]
verify_jwt = false

# Destino de webhook por tipo (lê URLs do banco — Admin Master).
[functions.comunicar-webhook-dispatch]
verify_jwt = false
