project_id = "lsfwmbpvithxqerfdlhy"

[functions.webhook-solicitacao]
verify_jwt = false

# Permite preflight OPTIONS sem JWT; a função valida o token manualmente.
[functions.admin-users]
verify_jwt = false

[functions.self-upgrade-plan]
verify_jwt = true

# Proxy para Evolution API (evita 403 do navegador → servidor Evolution).
[functions.evolution-proxy]
verify_jwt = false

# Proxy para webhook n8n (evita CORS do navegador → n8n).
[functions.n8n-comunicar-proxy]
verify_jwt = false
